#!/usr/bin/env bash

set -u

if command -v z13ctl >/dev/null 2>&1; then
    if ! output=$(z13ctl --device lightbar off 2>&1); then
        printf 'lightbar-off: %s\n' "$output" >&2
        exit 1
    fi
fi
